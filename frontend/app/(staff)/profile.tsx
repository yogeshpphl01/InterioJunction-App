/**
 * <route route="/(staff)/profile" role="admin|sales|factory">
 *   Thin binding to the shared <Profile/> screen (src/screens/Profile.tsx).
 * </route>
 */
import Profile from "@/src/screens/Profile";
export default Profile;
